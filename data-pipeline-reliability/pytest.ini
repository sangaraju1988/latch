[pytest]
# Keeps pytest from walking up to the parent `latch` repo's pyproject.toml
# (which configures pytest-asyncio, not installed/needed here) and treating
# these settings as this project's own.
testpaths = tests
